# Terraform-WebServer-Setup
Terraform Web Server Setup (AWS EC2) This project uses Terraform to provision an Apache HTTP web server on an AWS EC2 instance inside the default VPC. It also generates a secure SSH key pair, configures security groups, and automatically installs and starts Apache on the EC2 instance.
